server:
  port: 8086
  servlet:
    context-path: /
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/employee?useSSL=false&serverTimezone=Asia/Shanghai
    username: king
    password: lian
  jpa:
    show-sql: true # 显示sql 语句
    hibernate:
      ddl-auto: update
